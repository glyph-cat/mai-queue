export interface IUserPreferences {
  /**
   * Whether or not user wants notifications, this is not a representation of
   * whether or not the system permission has been granted.
   */
  allowNotifications: boolean
}
